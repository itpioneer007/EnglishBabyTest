# 英语宝模块检测 · 审查体系改造方案

> 目标：把「脚本审查」和「界面六维审查」从两个割裂流程，合并成一份「每题只审一次、结论可复用」的统一审查报告；脚本生成覆盖所有题型；前端审查结果 Tab 从"状态位展示"改成"结论报告"。

---

## 一、现状诊断（改什么，具体到文件）

| 问题 | 根因 | 位置 |
|------|------|------|
| 脚本只解析"有题干"的题 | `add()` 里 `if not stem: return`，听音题/纯音频题被跳过 | `scripts/common/gen_script.py` `QuestionCollector.add()` |
| 听力/口语/语音评测不生成脚本 | `finish_unit` 的 `gen_allowed` 白名单直接拦截 | `scripts/common/gen_script.py` `finish_unit()`（约 290-293 行） |
| 每次检测都重跑脚本审查 | 无"已审查"记忆，永远全量重审 | `web_server.py` `_run_llm_script_review()`（约 4650+ 行） |
| 前端"题干通过/内容通过"没信息量 | 六维卡片通过态只渲染"通过"二字 | `templates/index.html` 约 1382 行 `stTxt` |
| "需要连手机截图"废话文案 | `reasons` 默认值，未检维度硬塞进卡片 | `web_server.py` `_record_module_evidence()` 的 reasons 填充 |

**核心认知转变**：从"每次重跑一遍的巡检器"→"审查结果知识库 + 增量审查"。每个（模块+单元）只认真审一次，结论沉淀复用。

---

## 二、已具备的能力（勿重复造轮子）

改造前先知道这些基础已经存在，直接复用：

1. **扬声器内容获取（三种）** —— `scripts/common/asr.py`
   - ① 答错反馈页「听力内容：xxx」字段（`_extract_speaker_from_feedback`，在巧记单词模块）
   - ② 页面显示英文词（跟读题，`_speaker_word_of`）
   - ③ **scrcpy 数字直录 + ASR**（`start_scrcpy_record` / `wait_scrcpy_record` / `asr_transcribe`）
2. **ASR 已接入**：`qwen-audio-3.0-asr-flash-streaming`，`asr_transcribe()` 自动 ffmpeg 转 wav 16k 再识别，`compare_against_script()` 做扬声器词↔脚本 recording 比对
3. **scrcpy 直录封装**：`start_scrcpy_record(out_path, seconds, serial)` 已处理绝对路径/ADB 环境变量/残留进程清理/失败重试；`wait_scrcpy_record(proc, out_path)` 轮询文件大小稳定判定完成
4. **图片题识别**：`scripts/common/vision_fill.py` `recognize_image_question()`（脚本生成时补图片题选项）
5. **知识点 LLM 生成**：`finish_unit` 里 `_llm_knowledge()`（已要求面向小学生 30-70 字有说服力）
6. **App 题干↔脚本题干比对**：`web_server.py` `_record_module_evidence()` 内已实现（口语训练按 unit/big/stage_idx 序号匹配，巧记单词按内容匹配 `_script_match_qiaoji`）
7. **题数记录按 App N/M**：巧记单词 `_answer_loop` 已用 `extract_progress` 读"第1关 3/15"
8. **无试题关卡只记一次提示**：巧记单词 `_answer_loop` 已实现

---

## 三、改造方案（四层）

### 第 1 层：脚本生成 —— 所有"能确定答案"的题都进脚本

改 `QuestionCollector.add()` 的跳过条件：

- 从「`if not stem: return`」→ 改成「`if not stem and not answer and not recording: return`」
- 三类题的答案来源补全：

| 题型 | 答案/内容来源 | 复用能力 |
|------|-------------|---------|
| 选择题/判断题 | 选项 + 点击的正确答案 | 已有 |
| 听音选词/听音选释义 | 答错反馈页「听力内容」或 scrcpy+ASR | ✅ 已验证 |
| 图片题 | 视觉模型识别截图补选项 | 已有 vision_fill |
| 跟读/朗读题 | 页面英文单词 + recording | 已有 `_speaker_word_of` |

**取消 `gen_allowed` 白名单**：所有模块都生成脚本；听音题用 ASR 补 recording，图片题用视觉模型补内容。这样每个模块第一次跑完就有一份完整脚本。

### 第 2 层：脚本库 + 一次性审查（复用机制）

新增索引文件 `data/script_review_index.json`：

```json
{
  "湘少版(2024审定)/五上/巧记单词/U1": {
    "script": "260826湘少五上巧记单词U1.docx",
    "script_hash": "md5...",
    "reviewed_at": "2026-08-26",
    "questions": 15,
    "status": "done"
  }
}
```

**检测启动判断逻辑**（改 `_run_llm_script_review` 入口）：
1. （模块+单元）在索引 `status=done` 且脚本 hash 未变 → **跳过审查复用结论**，日志打"该单元已审查过，复用上次结论"
2. 不在索引 → 完整审查 → 结果写回索引
3. 脚本 hash 变了 → 重新审

### 第 3 层：统一审查模型 —— 把 8 条需求映射成统一检查结构

把用户列的 8 条题目审查需求落成统一维度（每道题一份结论，**结论必须是具体文本，不是状态位**）：

| 需求 | 统一维度 key | 判定来源 |
|------|-------------|---------|
| 1. 题干文字是否正确 | `stem` | 界面文本 + 脚本题干比对 |
| 2. 内容文字相符/完整/知识性/逻辑 | `content` | 脚本审查 LLM + 选项比对 |
| 3. 图片相符/完整/匹配/逻辑 | `image` | 视觉模型（有图才查） |
| 4. 能否正常作答/答案输入 | `answerable` | 界面作答检测 |
| 5. 答案/知识点/听力材料正确 | `answer_knowledge` | 答错反馈页 + 脚本 |
| 6. 听力音频完整/可播放/角色/相符 | `audio` | scrcpy+ASR + 播放检测 |
| 7. 能进下一题/流程完整 | `flow` | 关卡遍历是否卡顿 |
| 8. 报告页内容正确 | `report` | 报告页检查 |

**关键原则**：
- 每个维度落**具体结论文本**，例：`stem: "✅ 题干『听录音，选择与录音内容相符的释义』与脚本一致"`
- **不适用维度直接省略**（非听力题没有 audio 维度），不再填"需连手机截图"这类占位

### 第 4 层：前端 AI 审查结果 Tab 重做

1. **总览区**：`本次审查 15 题：通过 12 / 不通过 3 / 不适用 0`，下方列**不通过题清单**（题号+一句话原因），通过题默认折叠
2. **每道题卡片改成"结论式"**：
   - 通过：`Q01 听音选释义 · 题干与脚本一致 · 音频『new』正确`
   - 不通过：`Q05 看图填空 · ❌ App 图片是苹果，脚本应为香蕉（第1大题第3小题）` + 截图
3. **不适用维度直接隐藏**，删掉所有"需要连手机截图/需连手机"占位文案
4. **复用标记**：复用上次结论的单元，顶部显示"该单元 X 月 X 日已审查，本次复用结论（脚本未变）"

---

## 四、数据模型设计

### 1. 脚本审查索引 `data/script_review_index.json`

```json
{
  "<版本>/<年级>/<模块>/<单元>": {
    "script": "文件名",
    "script_hash": "md5",
    "reviewed_at": "日期",
    "questions": N,
    "status": "done|pending"
  }
}
```

### 2. 审查记录（写进 `data/inspection_state.json` 的每道题）

统一在 `_record_module_evidence` 写入的 question 里扩展，六维字段改造成带**具体结论**：

```json
{
  "stem":        {"status": "pass|fail|na", "detail": "题干与脚本一致：『...』"},
  "content":     {"status": "pass|fail|na", "detail": "..."},
  "image":       {"status": "pass|fail|na", "detail": "..."},
  "answerable":  {"status": "pass|fail|na", "detail": "..."},
  "answer_knowledge": {"status": "pass|fail|na", "detail": "..."},
  "audio":       {"status": "pass|fail|na", "detail": "扬声器『new』与脚本一致"},
  "flow":        {"status": "pass|fail", "detail": "..."},
  "report":      {"status": "pass|fail|na", "detail": "..."},
  "overall_passed": true
}
```

> 兼容：前端读取时，旧数据的 `ai_stem: true/false/null` 仍要能显示（迁移期并存）。

---

## 五、实施顺序（给实现者）

1. **改 `gen_script.py`**：`add()` 跳过条件放宽 + 取消 `gen_allowed` 白名单（听音题靠 ASR 补 recording，图片题靠视觉模型补）
2. **建 `data/script_review_index.json`**：读写两个小函数（`load_index()` / `save_index()`）
3. **改 `web_server.py` `_run_llm_script_review`**：入口加"查索引→复用 or 审查→写索引"
4. **统一审查模型**：改 `_record_module_evidence` 的六维 reasons → 八维"具体结论 + 不适用即省略"
5. **重做前端审查结果 Tab**：`index.html` 六维卡片渲染（约 1370-1415 行）→"总览 + 结论式卡片 + 折叠 + 复用标记"
6. **真机复跑一个模块**验证闭环（建议巧记单词 U1）

---

## 六、待拍板决策

1. **审查粒度**：索引 key 建议用「版本+年级+模块+单元」（同模块不同版本/年级题目不同，必须分开审）——默认按此执行
2. **脚本库目录**：自动生成的脚本建议单独放 `uploads/generated/`，`uploads/` 只放手动上传的原始脚本，避免混淆——默认按此执行

---

## 七、一句话总结

"脚本审查"和"界面六维审查"不该是两件事——它们是你那 8 条需求的两种取证手段，最终汇成一份"每题只审一次、结论可复用"的审查报告。前端展示的是报告的**结论**，不是"通过/不通过"的状态位。
