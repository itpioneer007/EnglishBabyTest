# (3)+(4) 实现状态分析 — 逐项对照

> Person A 职责 | 2026-07-23

---

## (3) 题目内容图片部分

### 3a. 图片是否与脚本相符

| 状态 | ⚠ 部分实现 |
|:--|:--|
| **已有方法** | `_detect_image_element()` |
| **做了什么** | 有 ADB 时通过 `dump_ui()` 搜 `image/img/pic` 资源 ID；没 ADB 时用 PIL 读文件大小（>50KB 算有图） |
| **还不够** | 这只是检测"有没有图"，不是检测"图对不对" |

**要补的**：
```
办法：用 LLM 视觉能力。
1. 把公司脚本里的"预期配图描述"传给 LLM
2. 同时传截图
3. LLM 对比：截图里的图和脚本描述是否一致
4. 目前 _image_match_prompt() 已经写了图文匹配 prompt，但还需要
   在 Question 数据结构里加一个 expected_image_desc 字段，
   存放公司脚本中对配图的描述。
```

### 3b. 图片是否显示完整

| 状态 | ✅ 已实现 |
|:--|:--|
| **已有方法** | `_is_image_truncated()` |
| **实现方式** | PIL 读取截图 → 取底部 30 像素 → 检测是否为纯白（白底=截断/空白） |

**怎么验证**：拿一张截断的图跑 `_is_image_truncated("test.png")`，应该返回 `True`。

**目前判断逻辑**：底部 30px 行里超过 80% 像素是白色 → 判定截断。

---

### 3c. 图片是否与题目内容匹配

| 状态 | ⚠ 部分实现 |
|:--|:--|
| **已有方法** | `_image_match_prompt()` + `LLMClient.ask()` |
| **做了什么** | 把题目题干 + 截图一起发给 LLM，让它判断图文是否匹配 |
| **还不够** | prompt 是通用模板，需要针对英语题调优 |

**要补的**：
```
1. 跑几道真实题目，看 LLM 回答质量
2. 如果 LLM 判断不准确，改 _image_match_prompt() 的 prompt:
   - 加更多英语题特例
   - 加判断标准（如"听力选图题中，图片应包含与录音相关的场景元素"）
3. 如果 LLM 返回格式不统一，加一个 _parse_llm_result() 解析
```

**当前 prompt（可改）**：
```python
你是英语教学插图质检专家。请分析这张截图中的图片是否与以下题目匹配。
题目: {q.stem}
题型: {q.question_type}
请判断：
- 图片内容是否与题干相关？
- 对于听力选图题，图片是否能表达听力场景？
```

---

### 3d. 图片是否有不合逻辑之处

| 状态 | ⚠ 部分实现 |
|:--|:--|
| **已有方法** | `_image_logic_prompt()` + LLM |
| **做了什么** | LLM 检查模糊/变形/颜色错误 |
| **还不够** | 同样的，prompt 需要调优 |

**要补的**：和 3c 一样，拿真实题目跑一遍调 prompt。

**当前 prompt（可改）**：
```python
请分析这张截图中的图片是否存在逻辑不合理之处。
题目: {q.stem}
检查点：
- 图片内容是否符合常识？
- 颜色/比例/文字是否有明显错误？
- 是否有模糊、变形等问题？
```

### 3e. 听力题 — 播放按钮

| 状态 | ✅ 已实现 |
|:--|:--|
| **已有方法** | `_detect_play_button()` |
| **实现方式** | ADB dump UI → 搜 `play` 资源 ID |

---

## (4) 检查题目是否能正常作答

### 4a. 选择题 — 选项能否点击

| 状态 | ✅ 已实现 |
|:--|:--|
| **已有方法** | `_check_choice_question()` |
| **实现方式** | ADB dump UI → 统计 y=700~1700 范围内的可点击元素数量 |
| **有 ADB** | 精确统计可选选项数 |
| **无 ADB** | 返回"无法检查"，但可改成 LLM 看图数选项 |

### 4b. 判断题 — T/F 能否点击

| 状态 | ✅ 已实现 |
|:--|:--|
| **实现方式** | 复用 `_check_choice_question()`，因为判断题本质也是选择题 |
| **触发** | `_guess_type()` 识别到"判断"关键词 → 走选择题逻辑 |

### 4c. 填空题 — 答案能否完整输入

| 状态 | ⚠ 部分实现 |
|:--|:--|
| **已有方法** | `_check_fill_question()` |
| **做了什么** | ADB dump UI → 搜 `edit/input/textfield` 资源 ID 找输入框 |

**还不够**：
```
当前只检测"有没有输入框"，没检测：
- 输入框数量是否够（比如 7 个字母的单词应该有 7 个框）
- 键盘能不能弹出来
- 输入后能不能正常回填

补法：
1. ✗ 无 ADB 模式：让 LLM 看图数输入框
2. ✗ 有 ADB 模式：实际点击输入框 → 检测键盘是否弹出
   （现在已有简化版键盘检测，在 inspection_engine 里）
```

### 4d. 口语题 — 能否以录音形式作答

| 状态 | ✅ 已实现 |
|:--|:--|
| **已有方法** | `_check_speaking_question()` |
| **实现方式** | ADB dump UI → 搜 `record/mic/speak/voice` 资源 ID |
| **局限** | 只检测按钮存在，无法验证录音功能是否真的能用（需要真实麦克风） |

### 4e. 排序题 — 能否正常操作

| 状态 | ⚠ 部分实现 |
|:--|:--|
| **已有方法** | `_check_sort_question()` |
| **做了什么** | ADB dump UI → 统计可操作元素数量（应 ≥ 排序项目数） |

**还不够**：
```
当前只统计可点击元素数量，没检测：
- 元素是否真的支持排序（clickable ≠ sortable）
- 拖拽排序是否可行

补法：无 ADB 模式用 LLM 看图判断布局是否合理
```

### 4f. 答案是否正确完整（跨题型）

| 状态 | ❌ 未实现 |
|:--|:--|
| **原因** | 确认答案正确需要**标准答案对比**，属于搭档的工作范畴 |

---

## 📊 总结

| 需求 | 状态 | 实现方法 | 待补 |
|:--|:----:|:--|:--|
| 3a 图片存在 | ✅ | PIL + ADB | — |
| 3b 显示完整 | ✅ | PIL 边缘分析 | — |
| 3c 图文匹配 | ⚠ | LLM 视觉 | **调 prompt** |
| 3d 不合逻辑 | ⚠ | LLM 视觉 | **调 prompt** |
| 3e 播放按钮 | ✅ | ADB dump | — |
| 4a 选择题作答 | ✅ | ADB clickable 统计 | 无ADB时的LLM替代 |
| 4b 判断题作答 | ✅ | 复用选择题 | — |
| 4c 填空题输入 | ⚠ | ADB 输入框检测 | 键盘弹出验证、无ADB模式 |
| 4d 口语题录音 | ✅ | ADB 按钮检测 | — |
| 4e 排序题操作 | ⚠ | ADB 元素统计 | LLM 布局分析 |
| 4f 答案正确性 | — | — | 搭档负责 |

**你的工作量最大的是 3c 和 3d**（调 LLM prompt），这两个也是审查的核心价值。其他的检测逻辑都已经写好了。
