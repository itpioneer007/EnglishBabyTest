# 双人协作分工文档

> 有效期: 2026-07-23 起 | 最后更新: 2026-07-23
> 
> **核心原则**: 每个人有自己的"独占文件"，另一个人永远不动。共享文件只在约定区域修改。

---

## 📂 文件归属一览

```
英语宝模块检测/
├── src/
│   ├── reviewer_common.py    ← 🔵 共享 (不要改，除非两人商量好)
│   ├── reviewer_text.py      ← 🟢 Person A 独占 (1+2)
│   ├── reviewer_media.py     ← 🟠 Person B 独占 (3+4)
│   ├── question_checker.py   ← 🟢 Person A 可改 (老代码，文字检查)
│   ├── inspection_engine.py  ← 🟠 Person B 可改 (老代码，图片+作答)
│   ├── ocr_engine.py         ← 🟢 Person A 可改 (OCR 相关)
│   ├── adb_controller.py     ← 🟠 Person B 可改 (ADB 操控)
│   ├── config_loader.py      ← 🔵 共享
│   └── flow_runner.py        ← 🔵 共享
├── web_server.py             ← 🔵 见下方「Web 文件约定」
├── templates/
│   └── index.html            ← 🔵 见下方「Web 文件约定」
└── data/
    └── sample_questions.json ← 🔵 共享测试数据
```

### 颜色说明

| 颜色 | 含义 | 规则 |
|:--:|:--|:--|
| 🟢 | Person A 独占 | Person B 不碰，只管调用 |
| 🟠 | Person B 独占 | Person A 不碰，只管调用 |
| 🔵 | 共享 | 只在自己标记的区域改，见下方约定 |

---

## 👤 Person A: 负责 (1) 题干 + (2) 内容

### 文件

| 文件 | 角色 | 说明 |
|:--|:--|:--|
| `src/reviewer_text.py` | 🟢 **你的核心文件** | `TextReviewer` 类，暴露 `check_stem()` + `check_content()` |
| `src/question_checker.py` | 🟢 可改 | 已有的旧代码，可提取有用逻辑 |
| `src/ocr_engine.py` | 🟢 可改 | OCR 提取文字，优化识别精度 |

### 你只需要关注

```python
from src.reviewer_text import TextReviewer
from src.reviewer_common import LLMClient, QuestionLoader

# 1. 加载公司给的题目文件
questions = QuestionLoader.load("公司给的文件.json")

# 2. 初始化审查器
reviewer = TextReviewer(llm=LLMClient(api_key="你的key"), script_questions=questions)

# 3. 逐题审查
for q in questions:
    stem = reviewer.check_stem(q, screenshot_path)
    content = reviewer.check_content(q, screenshot_path)
    # stem.passed / content.passed → 存入报告
```

### 你需要实现的功能

1. **题干 OCR 提取优化** — 在 `reviewer_text.py._extract_text()` 里调 PaddleOCR 或自有 OCR
2. **LLM Prompt 调优** — 在 `_knowledge_prompt()` 和 `_logic_prompt()` 里根据实际效果迭代
3. **脚本对比规则** — 如果公司文件格式特殊，在 `reviewer_common.py.QuestionLoader` 里加解析逻辑（必须告诉 Person B）

---

## 👤 Person B: 负责 (3) 图片 + (4) 作答

### 文件

| 文件 | 角色 | 说明 |
|:--|:--|:--|
| `src/reviewer_media.py` | 🟠 **你的核心文件** | `MediaReviewer` 类，暴露 `check_image()` + `check_answer()` |
| `src/inspection_engine.py` | 🟠 可改 | 已有的四步检查引擎，提取图片/作答逻辑 |
| `src/adb_controller.py` | 🟠 可改 | ADB 操控（仅在 ADB 模式需要） |

### 你只需要关注

```python
from src.reviewer_media import MediaReviewer
from src.reviewer_common import LLMClient, QuestionLoader

# 1. 加载题目
questions = QuestionLoader.load("公司给的文件.json")

# 2. 初始化审查器
reviewer = MediaReviewer(llm=LLMClient(api_key="你的key"))

# 3. 逐题审查
for q in questions:
    image = reviewer.check_image(q, screenshot_path)
    answer = reviewer.check_answer(q, screenshot_path)
    # image.passed / answer.passed → 存入报告
```

### 你需要实现的功能

1. **LLM 视觉 Prompt 调优** — `_image_match_prompt()` 和 `_image_logic_prompt()` 的 prompt 迭代
2. **ADB 模式下的 UI 检测** — 当有手机时，`check_answer()` 可以真实点选项验证
3. **图片截断/异常检测算法** — `_is_image_truncated()` 的 PIL 逻辑优化

---

## 🔵 Web 文件约定（防止冲突）

### `web_server.py` (1204 行)

在文件底部按以下格式添加路由：

```python
# ============================================================
# 🔵 Person A 路由区 —— 题干 + 内容审查
# ============================================================

@app.route("/api/review-text", methods=["POST"])
def api_review_text():
    ...

# ============================================================
# 🔵 Person B 路由区 —— 图片 + 作答审查
# ============================================================

@app.route("/api/review-image", methods=["POST"])
def api_review_image():
    ...
```

**规则**: 只在各自的路由区加代码，不要互相跨越。

### `templates/index.html` (574 行)

在 `<body>` 最后加以下结构：

```html
<!-- ============================================================ 
     Person A 面板: 文字审查
     ============================================================ -->
<div class="card" id="personA">
  ...
</div>

<!-- ============================================================ 
     Person B 面板: 图片+作答审查
     ============================================================ -->
<div class="card" id="personB">
  ...
</div>

<script>
// ========== Person A JS 区 ==========
function reviewText() { ... }

// ========== Person B JS 区 ==========
function reviewImage() { ... }
</script>
```

---

## 🚀 Git 协作流程

### 每天开始

```bash
git pull origin main        # 拉最新代码
```

### 开发时

```bash
# Person A 只改自己的文件
git add src/reviewer_text.py src/question_checker.py src/ocr_engine.py

# Person B 只改自己的文件
git add src/reviewer_media.py src/inspection_engine.py src/adb_controller.py

# 共享文件需要两人确认后才改
git add src/reviewer_common.py web_server.py templates/index.html
```

### 提交前

```bash
# 确认只有自己的文件被修改
git status

# 如果发现动了对方的文件 → 赶紧复原
git checkout -- src/reviewer_text.py   # Person B 误改了 Person A 的文件
```

### 冲突处理

如果 `web_server.py` 冲突了：
- 找到各自的区域标记 (`Person A 路由区` / `Person B 路由区`)
- 保留两边的代码
- 不要删掉对方的行

---

## ✅ 你接下来要做的

| 步骤 | Person A | Person B |
|:--:|:--|:--|
| 1 | 读 `src/reviewer_text.py` | 读 `src/reviewer_media.py` |
| 2 | 在 `_extract_text()` 里接上你的 OCR | 在 `_check_*()` 方法里接上你的 ADB |
| 3 | 调 `_knowledge_prompt()` prompt | 调 `_image_match_prompt()` prompt |
| 4 | 用 `data/sample_questions.json` 做测试 | 用同一样本做测试 |
| 5 | 在下方的 Person A 路由区加 `/api/review-text` | 在下方的 Person B 路由区加 `/api/review-image` |
| 6 | 在 `index.html` 的 Person A JS 区加前端 | 在 `index.html` 的 Person B JS 区加前端 |

---

## 📞 需要对方做什么？

| 你需要 Person B 提供 | 你需要 Person A 提供 |
|:--|:--|
| — | `QuestionLoader` 已写好（共享文件） |
| — | `LLMClient` 已写好（共享文件） |
| — | `CheckItem` 数据格式（共享文件） |

**双方不直接依赖彼此的代码**——通过 `reviewer_common.py` 里的共享数据结构交互。

---

_这份文档是活的。如果有新情况，随时更新。_
