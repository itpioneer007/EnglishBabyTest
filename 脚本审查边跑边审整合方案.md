# 脚本审查「边跑边审」整合实施方案

> 交付实施智能体使用，自包含、可直接落地。

## 0. 目标（一句话）

把当前「跑完模块后再分离审查脚本」改成「**每道题在跑的过程中，基础层(UI证据) + 脚本层(对照docx) 同时产出，合并成同一条审查记录**；无脚本时只做基础层」。

---

## 1. 现状与问题（实施前必读）

| 管线 | 触发点 | 写入 key | 说明 |
|------|--------|---------|------|
| 基础层 | 实时，`web_server.py::_record_module_evidence` (约 L2989) | `auto-Q{qidx:03d}` 或 `auto-Q{big:02d}-{qidx:02d}` | UI 证据六维 |
| 脚本层 | 跑完后异步，`web_server.py::_run_llm_script_review` (约 L4894) | `{module}-脚本-Q{idx:02d}` | LLM 对照 docx 六维 |

**三个核心事实（方案复用，不用重做）**：

1. 单题脚本审查能力**已存在**：`ReviewAgent._review_one` (src/review_agent.py 约 L290)。
   签名：`_review_one(q, screenshot, post_error_shot="", is_first_question=False, ui_texts=None, detected=None) -> QuestionReview`，含六维 + 知识库。
2. 基础层里**已经内嵌了脚本匹配**：`web_server.py` 约 L3116-L3217，通过 `_GLOBAL_DOCX_MAP` + `_SCRIPT_CACHE` 定位到脚本题 `_sq_cur`，但目前**只做题干+音频两个维度**，没做完整六维。
3. 脚本解析缓存已存在：`_SCRIPT_CACHE`，避免 100 题重复 parse。

---

## 2. 目标数据结构

`_inspection_state["questions"][qid]` **一条记录**同时承载两层：

```python
{
    # —— 基础层（UI证据，实时，必做）——
    "idx":..., "module":..., "stage":..., "position":"U3·第2大题·第4小题",
    "stem":bool/None, "content":..., "image":...,
    "answer":..., "audio":..., "post_error":...,
    "stem_reason":"...", "content_reason":"...", ...

    # —— 脚本层（对照docx，有脚本才做；无脚本=None）——
    "script": {
        "matched": true,          # 是否匹配到脚本题
        "stem": bool/None,        # 题干一致性
        "content": bool/None,     # 选项一致性
        "answer": bool/None,      # 答案正确性
        "knowledge": bool/None,   # 知识点正确性
        "audio_material": bool/None, # 听力材料一致性
        "reason": "..."           # LLM 汇总结论
    },
    "layer": "basic_only" | "basic+script",   # 前端分层渲染用
}
```

---

## 3. 实施步骤（按顺序执行）

### 步骤 1 —— 确认/稳定单题审查入口（src/review_agent.py）

`_review_one` 已可直接单题调用，**无需改动**，只需保证实现方知道：

- 传 `screenshot=""`（无图）时仍会走 `_review_script_llm` 逐维 LLM 判定，配图/音频标 skip —— 这正是"脚本层"要的行为。
- 返回的 `QuestionReview` 用 `_qreview_to_state`（web_server.py 约 L4818）转 `script` 子结构。

### 步骤 2 —— `_record_module_evidence` 扩展脚本层（**核心**）

在 web_server.py 约 L3161 `if _sq_cur:` 分支里，把当前"只做 stem/audio 比对"扩展为"**派发完整脚本层审查**"。

具体做法（伪代码）：

```python
# —— 现有逻辑：基础层六维实时写（保持不变）——
# ... dims/reasons 基础层判定 ...

# —— 新增：脚本层（异步回填到同一条记录）——
if _sq_cur and not script_reviewed(qid):          # qid 见步骤2末尾
    mark_layer(qid, "basic+script")                # 先标"审查中"
    submit_async_task(                             # 异步线程池，不阻塞答题
        _review_one(_sq_cur, screenshot=auto_shot, ui_texts=ui_texts, detected=detected),
        on_done=lambda r: merge_script_layer(qid, _qreview_to_state_script(r))
    )
else:
    mark_layer(qid, "basic_only")                  # 脚本层字段全 None
```

要点：

- **异步**：`_review_one` 每题 2~5s LLM，不能同步阻塞答题推进；基础层先写，脚本层审完回填同一条记录。
- **回填用相同 qid**：基础层和脚本层写进 `questions[qid]["script"]`，而不是新建 `{module}-脚本-Qxx` 记录。
- **`_qreview_to_state_script(r)`**：从 `_qreview_to_state` 抽一个只取六维 + knowledge 的轻量版，填进 `script` 子结构。

### 步骤 3 —— `_run_llm_script_review` 降级为「兜底补审」

原函数（web_server.py 约 L4894）改为只补两类题，**且回填到基础层记录**而非新建 key：

```python
# 兜底1：边跑时没走实时审查的题（如脚本题未出现在UI里）→ 补审
# 兜底2：脚本层审查中"uncertain/待审"的题 → 二次确认
# 回填：结果按 (module, stage, 位置) 匹配到 questions[qid]["script"]
```

`_on_module_done` 回调（web_server.py 约 L5144）保留，但触发的不再是"全量独立审查"，而是"兜底补审"。

### 步骤 4 —— 缓存索引题级化

web_server.py 约 L4932 索引 key 从模块级细化为题级：

```
旧: {version}/{grade}/{module}/{unit}
新: {version}/{grade}/{module}/{unit}/Q{idx}
```

单题命中即复用，避免"同一模块重跑却因没跑完而全部重审"。

### 步骤 5 —— 前端分层渲染（templates/index.html）

改 index.html 约 L1356-L1511 结论式卡片为两段：

- **基础层**（UI 完整性）：始终显示，现有样式。
- **脚本层**（脚本一致性）：`layer == "basic+script"` 才显示，独立底色/小标题「脚本核验」，审查中显示 loading，审完显示结论。
- **总览基数** = 基础层题数（无脚本时也在审）；脚本层通过/不通过只作为每题的附加标签。

---

## 4. 复用闭环（已存在，无需新增）

```
跑模块 → gen_script.finish_unit() 生成 docx 到 gen_scripts/ 目录
       → 用户手动上传到 uploads/
       → 下次跑同模块 → _GLOBAL_DOCX_MAP 命中
       → 自动启动脚本层（边跑边审）
```

改造后唯一保证：脚本层不依赖"跑完"时机，**只要上传了脚本，任何一次跑都触发**。

---

## 5. 边界情况（实现必须覆盖，均有现成代码可复用）

| 情况 | 现成处理 | 位置 |
|------|---------|------|
| 巧记单词抽题（无序号） | `_script_match_qiaoji` 内容匹配 | web_server.py 约 L3093 |
| 口语训练大题/小题定位 | `(unit, big, stage_idx)` | web_server.py 约 L3151 |
| 听力专项 stage | `_run_llm_script_review` stage 参数 | web_server.py 约 L4899 |
| 图片/听音题维度无法核对 | `_cr()` 的 skip/uncertain → 计「未检」 | web_server.py 约 L4842-L4849 |
| 脚本与模块名不匹配 | `_mod_kws` 白名单校验 | web_server.py 约 L4913 |
| 题号去重（防覆盖） | `_seen_qids` dict 去重 | web_server.py 约 L3268 |

---

## 6. 改动文件清单

| 文件 | 改动级别 |
|------|---------|
| `web_server.py` | 核心：`_record_module_evidence` 扩展脚本层；`_run_llm_script_review` 降级兜底；索引题级化 |
| `src/review_agent.py` | 无需改（`_review_one` 已具备；可选：抽 `review_one_async` 便捷入口） |
| `templates/index.html` | 卡片两段式 + `layer` 标记渲染 + 总览基数调整 |
| `data/script_review_index.json` | 结构 key 加 `Q{idx}` 层级（自动生成，无需手工改） |

---

## 7. 验收标准（实现完成后逐条核对）

1. ✅ 无脚本跑模块：每道题只有基础层六维，`script` 层为 `basic_only`，前端无脚本核验区块。
2. ✅ 有脚本跑模块：每道题基础层实时出现，脚本层在数秒内异步回填到**同一条记录**。
3. ✅ 脚本层通过/不通过不影响基础层通过/不通过的独立展示。
4. ✅ 图片题/听音题的脚本层不可核维度显示「未检」而非「不通过」。
5. ✅ 重跑同模块：已审过的题命中题级索引，秒出不复审。
6. ✅ 巧记单词/口语训练/听力专项三模块分别验证定位与 stage 正确。