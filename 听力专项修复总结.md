# 听力专项 自动点击 + 自动做题 修复总结

## 一、之前为什么不行

### 1. 年级被写死成 "五年级上册"
- 引擎文件 `scripts/modules/听力专项.py` 第 38 行：`GRADE_LEVEL = "五年级上册"`
- Web 接口 `web_server.py` 第 4218 行：`ensure_grade(d, "五年级上册", "湘少版")`
- 五年级上册的听力专项在当前 App 版本下**内容为空 / 正在开发**，所以点进去没有「去练习」入口，更谈不上做题。
- 你的目标年级其实是 **六年级上册**（`config.yaml` 里的 `image_dir` 也指向六上 U6-9）。

### 2. `u2.connect()` 没有传 serial
- `web_server.py` 的 `_connect_device()` 原来用裸 `u2.connect()`。
- 你当前电脑同时连了 2 台 ADB 设备，`u2.connect()` 会报 `more than one device` 直接连不上设备。

---

## 二、改了什么

| 文件 | 改动 | 原因 |
|------|------|------|
| `scripts/modules/听力专项.py` 第 38 行 | `GRADE_LEVEL = "六年级上册"` | 让引擎从命令行单跑时默认进六上 |
| `web_server.py` 第 4218 行 | `ensure_grade(d, "六年级上册", "湘少版")` | Web 启动听力专项任务时切到六上 |
| `web_server.py` 第 614 行 | `u2.connect(config.device.serial)` | 多设备环境下也能正确连接当前设备 |

---

## 三、验证结果

- `python -m py_compile web_server.py` 通过。
- `python -m py_compile scripts/modules/听力专项.py` 通过。
- `from modules.听力专项 import run_module, run_test_module` 成功导入。
- `GRADE_LEVEL` 打印为 `六年级上册`。
- 真机年级切换测试确认可切到：湘少版（2024审定）六年级上册。

---

## 四、自动做题的真相

项目里已经有一个完整的听力专项引擎（`scripts/modules/听力专项.py` + `engine.py` 的答题循环），它能：

- 自动导航：首页 → 找到「听力专项」→ 点击进入。
- 自动遍历单元：`Unit 1` ~ `Unit 9` 练习，`Unit 1` ~ `Unit 5` 测试。
- 自动识别题型：T/F、A/B/C/D、排序、匹配、填空、图片题等，自动点击选项并点「检查」推进。
- 自动处理子模块：基础巩固 → 综合进阶 → 难点突破（练习模式）。

### 限制

**听力题要"做对"必须听懂录音**，而通过 ADB 无法可靠地采集 App 内部音频。
所以当前效果是能**自动把题做完**（机械答题、翻页、提交），但**不能保证答案都对**。
如果想实现真正"做对"的听力，需要额外部署：
1. 手机内部音频采集能力（一般需要 root 或特殊外接硬件）；
2. ASR 语音识别；
3. 与题干语义对比后判断 T/F 或选择答案。

---

## 五、怎么用

在 `EnglishBabyTest-yangjiangliu` 目录启动 Web 服务：

```powershell
cd C:\Users\yangj\Desktop\EnglishBabyTest-yangjiangliu
D:\Drivers\Audio\python.exe web_server.py
```

然后浏览器打开 `http://localhost:5000`，在面板里启动「听力专项」任务。

也可以直接调用接口：

```bash
curl -X POST http://localhost:5000/api/audio/run \
  -H "Content-Type: application/json" \
  -d '{"mode":"all","units":[1],"test_units":[1]}'
```

`mode` 可选 `all`、`practice`、`test`。

---

## 六、注意事项

- 首页/模块页常有广告浮层（例如误触后出现的「E英语宝伴学服务」二维码页），uiautomator 可能读不到这些浮层，导致点入口时误触广告。引擎已用 `settle_ads`、`dismiss_global_popups`、`applock_blocked` 多层防护，但遇到新型未识别广告时仍可能被打断。
- 如果运行过程中被弹窗挡住，先在手机上关掉弹窗，再点 Web 面板里的「停止」并重新运行。
- 设备号请保持 `config.yaml` 里 `device.serial` 与当前在线设备一致（当前是 `192.168.2.96:40457`）。
